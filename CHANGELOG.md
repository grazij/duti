## 1.5.5+grazij.2

### Bug Fixes

* stop the changelog silently dropping most of the fork's commits (09e51ff)

## 1.5.5+grazij.1

### BREAKING CHANGES

* replace the settings_path operand with -c, and fork the version scheme (b25ee9a)

### Features

* replace the settings_path operand with -c, and fork the version scheme (b25ee9a)

### Bug Fixes

* run CI on master update only (1b936c0)
* corrected link (81ccfd3)
* automated versioning and releases in CI (a99d92f)

### Other Changes

* restore the pull_request trigger (349e86f)
* Ignore the built duti binary (c6b5ebb)
* Document the -e and -u options (657f688)
* Document the build requirements and the macOS 11 deployment target (a8cf595)
* Strip whitespace when reading the version out of version.toml (022a003)
* Set a fixed macOS 11 deployment target instead of tracking the host (6543fc0)
* Add support for macOS 14, 15, and 26 (5c99a68)
* Add validation for dynamic UTI registration (dd801ba)

